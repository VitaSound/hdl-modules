# {{ repo.name }}

{{ repo.description_ru }}

Наработки постепенно переосмысливаются и переносятся из [fpga-synth]({{ repo.legacy_source }})

# назначение модулей

## {{ common.title_ru }}

[{{ common.subtitle_ru }}]({{ common.readme }})

| N | Module | Description | Img |
| - | ------ | --- | --- |
{% for module in common.modules %}
| {{ loop.index }} | {{ module.id }} | {{ module.description_ru }} | ![{{ module.id }}]({{ module.image }}) |
{% endfor %}

{{ generation.title_ru }}

| N | Module | Description | Img |
| - | ------ | --- | --- |
{% for package in generation.packages %}
| {{ loop.index }} | [{{ package.id }}]({{ package.readme }}) | {{ package.description_ru }}{% if package.variants %} <br><br>{% for variant in package.variants %} [{{ variant.file | basename }}]({{ variant.file }}) - {{ variant.label_ru or variant.label }} <br>{% endfor %}{% endif %} | ![{{ package.id }}]({{ package.image }}) |
{% endfor %}

# окружение

Все модули разработаны и протестированы в icarus verilog

{{ repo.iverilog_guide }}

для просмотра gtkwave

## Автоматизация

```bash
make test      # запустить все симуляции
make images    # сгенерировать waveform PNG
make docs      # обновить README из modules.yaml
make all       # test → images → docs
```

Как добавить новый модуль: [docs/ADDING_MODULES.md](docs/ADDING_MODULES.md)

Зависимости: [docs/DEPENDENCIES.md](docs/DEPENDENCIES.md)

{% if repo.daw %}
## {{ repo.daw.title_ru }}

{{ repo.daw.intro_ru }}:

{% for link in repo.daw.links %}
- {{ link.name }}: [{{ link.path }}]({{ link.path }}){% if link.hint %} — {{ link.hint }}{% endif %}

{% endfor %}
{% endif %}

# sandbox

песочница со всем подряд

самый первый сайт

{{ repo.sandbox_link }}

# заметки

sudo apt-get install libasound-dev portaudio19-dev libportaudio2 libportaudiocpp0

sudo apt-get install libasound-dev verilator

sudo apt install --reinstall alsa-base alsa-utils

<!-- generated by tools/gen_readme.py -->
