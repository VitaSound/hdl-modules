---
description: hdl-modules workflow, modules.yaml, make pipeline, adding modules
alwaysApply: true
---

# hdl-modules

Полный гайд: [AGENTS.md](AGENTS.md). Добавление модуля (людям): [docs/ADDING_MODULES.md](docs/ADDING_MODULES.md). Планирование изменений — OpenSpec в `openspec/` (`/opsx-propose`).

## Пайплайн

- Метаданные → `modules.yaml` (не дублировать в README вручную).
- `make test` / `make images` / `make docs` / `make all`; CI проверяет тесты, smoke `make images`, и git diff только README/AI_INDEX.md/modules.yaml (не PNG).
- README и `AI_INDEX.md` только через `make docs` (файлы с `<!-- generated by tools/gen_readme.py -->`).
- PNG: `{test_dir}/test.png`; вид волн — `test.gtkw` (zoom + analog format). Рендер не делает Zoom Full при наличии gtkw.
- `test.sh` → `python3 tools/run_tests.py --id <id>`.
- `verilator_tests/` — отдельно, не в `make all`.

## Добавить модуль (кратко)

1. RTL + `test/testbench.v` + `test.sh`
2. `make sim ID=<id>`
3. `make wave ID=<id>` → сохранить `test.gtkw`
4. Запись в `modules.yaml` (`common.modules` или `generation.packages`)
5. `make all` → закоммитить `.v`, testbench, gtkw, png, yaml, README, `AI_INDEX.md`

## Экосистема

[MIT](https://github.com/VitaSound/MIT) — методология; [feco](https://github.com/VitaSound/feco) — Forth-тулчейн; [fhdlgen](https://github.com/VitaSound/fhdlgen) — генератор Verilog из Forth. Индекс модулей для внешних репо — `AI_INDEX.md`.

## Стиль

Минимальный diff; как соседние `.v`; не коммитить `out.vcd`, `testbench`, `obj_dir/`.
